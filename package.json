{
  "name": "brick-breaker-game",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "compile": "cross-env HARDHAT_IGNORE_NODE_VERSION=1 hardhat compile",
    "deploy:base": "node scripts/deploy.js",
    "deploy:testnet": "node scripts/deploy.js",
    "verify:base": "cross-env HARDHAT_IGNORE_NODE_VERSION=1 hardhat verify --network base"
  },
  "dependencies": {
    "ethers": "^6.13.0",
    "solc": "^0.8.35"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.13",
    "cross-env": "^10.1.0",
    "dotenv": "^17.4.2",
    "hardhat": "^3.9.0",
    "vite": "^5.2.0"
  }
}
